| Функция | Вход | Выход | Описание |
|----------|------|--------|-----------|
| load_tasks() | — | List[dict] | Читает tasks.json, если нет — [] |
| save_tasks(tasks) | List[dict] | None | Записывает задачи в JSON |